why remote repo?

local report to  repo:
methed : 1
git - repo create
enter
git clone <link>
enter
git add . / git  add index.html 
enter
git  commit -m  "msg"
enter
genterit push origin mainenter
enter 

# tailwind css  cli 
<h1>hello! Welcome our Website </h1>
1. copy -npm install tailwindcss @tailwindcss/cli
2. css - @import "tailwindcss";
3. package.json - npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch

<h1>vs code</h1>
1. open terminal.
2. paste cmd.
3. Enter.
4. create index.html and input.css.
5. npm run start.
6. npm dev start.
7. input.css(copy) - @import "tailwindcss";
8. package.json(copy)-"scripts": {
    "start":"npx @tailwindcss/cli -i ./input.css -o ./output.css --watch"
  }
9.index.html(copy)-<link rel="stylesheet" href="./output.css">


---how many  branch  in repo
git branch 
git branch <branch name >
ex.git branch development 
git branch 
git checkout <CHECKOUT NAME >
ex.git checkout development